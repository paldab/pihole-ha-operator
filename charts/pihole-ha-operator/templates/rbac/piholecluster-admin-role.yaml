{{- if .Values.rbac.helpers.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "pihole-ha-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "pihole-ha-operator.resourceName" (dict "suffix" "piholecluster-admin-role" "context" $) }}
rules:
- apiGroups:
  - pihole.paldab.nl
  resources:
  - piholeclusters
  verbs:
  - '*'
- apiGroups:
  - pihole.paldab.nl
  resources:
  - piholeclusters/status
  verbs:
  - get
{{- end }}
